{
  services = {
    mpd = {
      enable = true;
      user = "beta";
      settings = {
        bind_to_address = "/home/beta/.mdp/socket";
        music_directory = "/home/beta/Music";
        playlist_directory = "/home/beta/.mpd/playlists";
        db_file = "/home/beta/.mdp/db";
        audio_output = [ 
          {
            type = "pipewire";
            name = "PipeWire Output";
          }
          #{
          #  type = "alsa";
          #  name = "ALSA Null";
          #  mixer_type = "null";
          #}
        ];
      };
    };
  };
}
